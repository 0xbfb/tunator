# Supported Configs

Modo básico: `SOCKSPort`, `ControlPort`, `DataDirectory`, `Log`, `ExcludeNodes`, `CookieAuthentication`.

Modo avançado: `GeoIPFile`, `GeoIPv6File` e opções sensíveis via `advanced_mode=true`.
