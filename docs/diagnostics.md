# Diagnósticos

O endpoint `/api/diagnostics/run` executa checagens estruturadas (binário, torrc, portas, ControlPort, bootstrap e onions).

Resultados ficam persistidos no SQLite para comparação de execuções recentes.
